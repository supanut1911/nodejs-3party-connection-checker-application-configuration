apiVersion: v1
kind: Service
metadata:
  name: thirdparty-connection-checker
  namespace: nodejs-checker-argocd

  
spec:
  type: ClusterIP
  ports:
  - port: 4455
  selector:
    app: thirdparty-connection-checker